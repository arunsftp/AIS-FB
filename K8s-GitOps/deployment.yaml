apiVersion: apps/v1
kind: Deployment
metadata:
  name: ais-fb-app
  namespace: ais-fb-dev
  labels:
    app: ais-fb-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ais-fb-app
  template:
    metadata:
      labels:
        app: ais-fb-app
    spec:
      containers:
      - name: myapp
        image: arunatg/test-build:latest
        ports:
        - containerPort: 3000
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "200m"
        env:
        - name: ENVIRONMENT
          value: "production"
      restartPolicy: Always
